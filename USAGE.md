# دليل الاستخدام السريع

## خطوات التشغيل

### 1. التشغيل في وضع التطوير

1. افتح المشروع في VS Code
2. اضغط `F5` أو اذهب إلى **Run > Start Debugging**
3. سيفتح نافذة جديدة: **[Extension Development Host]**

### 2. اختبار الإضافة

في نافذة Extension Development Host:

1. افتح ملف `test.html` الموجود في المشروع
2. اضغط `Cmd+Shift+P` (أو `Ctrl+Shift+P` على Windows)
3. ابحث عن: **WYSIWYG: Open HTML Visual Editor**
4. اضغط Enter

سيُفتح المحرر المرئي بجانب محرر النصوص!

### 3. اختبار الميزات

#### أ. التزامن من المحرر إلى الملف

- عدّل أي نص في المحرر المرئي
- لاحظ تحديث الملف تلقائياً (انتظر 300ms)

#### ب. التزامن من الملف إلى المحرر

- عدّل أي نص في الملف مباشرة
- لاحظ تحديث المحرر المرئي تلقائياً

#### ج. إدراج صورة

1. انسخ أي صورة من أي مكان
2. الصقها في المحرر (Cmd+V / Ctrl+V)
3. ستُحفظ الصورة في مجلد `assets/` تلقائياً
4. سيُضاف `<img src="assets/img_xxx.png">` في الملف

### 4. التأكد من عمل CKEditor

عند فتح المحرر، يجب أن ترى:

- ✅ شريط أدوات كامل في الأعلى
- ✅ منطقة تحرير قابلة للكتابة
- ✅ شريط حالة يقول "محرر HTML المرئي - جاهز"
- ✅ دعم العربية والـ RTL

### 5. حل المشاكل

#### المحرر لا يفتح

- تأكد من أن الملف المفتوح هو HTML
- تحقق من Console في Developer Tools: `Help > Toggle Developer Tools`

#### CKEditor لا يظهر

- تأكد من وجود مجلد `vendor/ckeditor/`
- تحقق من وجود ملف `ckeditor.js` داخله
- أعد البناء: `npm run build`

#### الصور لا تُحفظ

- تأكد من صلاحيات الكتابة في مجلد المشروع
- تحقق من Console للأخطاء

### 6. البناء للإنتاج

```bash
npm run build
```

سيُنشئ ملف `dist/extension.js` جاهز للنشر.

### 7. إنشاء Package للنشر

```bash
# تثبيت vsce إذا لم يكن مثبتاً
npm install -g @vscode/vsce

# إنشاء package
vsce package
```

سيُنشئ ملف `.vsix` يمكن تثبيته في VS Code.

---

## نصائح الاستخدام

- 💡 يمكنك فتح عدة محررات مرئية لملفات HTML مختلفة
- 💡 التزامن يحدث تلقائياً ولا يحفظ الملف - احفظه يدوياً (Cmd+S)
- 💡 استخدم أزرار CKEditor لإضافة جداول، قوائم، روابط، وغيرها
- 💡 يمكنك التبديل لعرض المصدر في CKEditor (زر Source)

---

## الاختصارات المفيدة

- `F5` - بدء التصحيح (Debug)
- `Cmd+Shift+P` - فتح لوحة الأوامر
- `Cmd+S` - حفظ الملف
- `Cmd+Z` - تراجع
- `Cmd+Shift+Z` - إعادة

---

## الدعم

إذا واجهت مشكلة، افتح Issue على GitHub أو راجع الـ Console للأخطاء.
